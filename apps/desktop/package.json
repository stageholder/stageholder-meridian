{
  "name": "desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wait-on http://localhost:3000 tcp:localhost:4000 && tauri dev",
    "build": "tauri build",
    "build:production": "tauri build --config '{\"build\":{\"frontendDist\":\"'\"${MERIDIAN_FRONTEND_URL:-https://app.meridian.example.com}\"'\"}}'",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-log": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "wait-on": "^8.0.0"
  }
}
